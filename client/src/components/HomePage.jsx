import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const HomePage = () => (
  <Card className="container">
    <CardTitle title="Bienvenido a Comparte Tu Mesa" subtitle="Crea una cuenta o inicia sesión para empezar." />
  </Card>
);

export default HomePage;
